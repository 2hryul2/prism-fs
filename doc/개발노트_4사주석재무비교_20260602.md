# 개발노트 — prism-fs (4대 지주 주석·재무제표 비교 대시보드) · 2026-06-02

## 2026-06-02 — A(개발노트)·C(코드정리)·B(빌드·배포) 완료

이전 세션(20260601)에서 A~D(P1~P7) 백엔드·UI 완료. 이번 세션은 후속 E·F 마무리.

### A. 개발노트 갱신 — 커밋 `7a681a2`
- `doc/개발노트_..._20260601.md` 에 P4~P7·축전환·별도주석·A~D 내역 62줄 추가(누락분 보완).

### C. 코드 정리 — 커밋 `fb54352`·`7e61f27`·`51bff74`
- **dormant 제거**: `index.html` 의 미사용 wordmap JS 블록(존재하지 않는 `/api/wordmap/*` 호출) 143줄 삭제(1494→1351). `goCompareTopicAll`·`loadInsight` 는 커버리지 탭 실사용이라 보존(개별 검증).
- **`safety.py` 중앙화**: `mask_secrets`/`safe_err`(DART/OpenAI/sk-*/Bearer) + `provenance` 표준 헬퍼. `app._safe_err` 위임, `fs_compare` 5개 provenance 경유. `tests/test_safety.py`(4) 추가.
- preview eval 로 wordmap `undefined`·이후 함수 정상 정의(JS 무결성)·콘솔 0 확인.

### B. 빌드·배포 — 커밋 `41cb240`·`b84220d`·`1499967`
- **경로 중앙화 `paths.py`**: dev·PyInstaller frozen 일관 해석(BUNDLE_DIR/APP_DIR/STORAGE_ROOT). app·fs_compare·notes_rag·collect_dart 의 `__file__` 기반 storage 경로를 paths 경유로 교체. frozen 시 동봉 모델·exe 옆 영속 storage 분리. dev 경로 불변.
- **Playwright E2E** `tests/e2e/test_smoke.py`: 라이브러리→재무제표 8뷰→비교→커버리지 탭 전환 + vendored pdf.js→canvas 렌더 + 콘솔 error 0. 서버 미가동 시 graceful skip. **2 passed**.
- **PyInstaller onedir 풀번들**: `run_server.py`(진입점·브라우저 자동오픈·오프라인 강제) + `prism_fs.spec`(torch+ST+ko-sroberta 동봉, VERSION 주입) + `build.ps1`(게이트·storage 동봉, ASCII 전용=PS5.1 호환).
- **빌드 산출물**: `dist\setup\setup_v0.1.0\setup_v0.1.0.exe`(87MB 런처) + `_internal`(torch·모델) + `storage`(4사). 총 **5.95GB**.
- **검증(exe 실기동)**: 번들 모델 오프라인 로드(`_internal\models\ko-sroberta`)·storage(lib=12)·fs/ratio(1228.55)·PDF(2.1MB)·index.html(200) 전부 정상.

### 결정 사항
- 번들 전략 = **풀번들(onedir)** (사용자 선택). 경량 onefile 은 인덱스 768차원 ↔ bigram 512차원 불일치로 임베딩 검색이 깨져 배제.
- `build.ps1`·`prism_fs.spec` ASCII 전용 — PS 5.1 의 BOM 없는 비ASCII .ps1 파싱 오류 회피.
- `.claude/settings.local.json`·`handoff/`·`src/models/`·`build/`·`dist/` gitignore.

### 검증
- `python -m pytest tests -q` → **32 passed**(safety 4 + e2e 2 포함). 외부 https 0·콘솔 0.

### 미완료/다음 작업
- **PDF 검색 정확도 개선**(신규 워크스트림, 승인됨): Phase D(추출 정밀화)→A(본문 청크 인덱싱)→B(kiwi 토크나이저)→C(top-K 리랭킹)→E(동의어). 인덱스 스키마 v2 + 12셀 재인덱싱 + exe 재빌드.
- (선택) 2025FY 수집, OpenAI 키 실호출 검증.

### 이슈/특이사항
- 번들 5.95GB(torch CUDA 라이브러리 포함) — 추후 CPU-only torch 로 용량 축소 검토 가능.
- 서버는 :8021 단일 유지(중복 기동 금지). exe 와 dev 서버는 같은 포트라 동시 기동 불가.

## 2026-06-02 (오후) — PDF 검색 정확도 개선 (Phase D·A·B·C·E)

사용자 지목 4대 부정확(본문질의 미스·오답 상위·추출오류·동의어 누락) 전부 대응. 제목-only
인덱싱 → **본문 내용 매칭 + 정밀 인용**으로 전환. 안전경계(텍스트 전용·숫자 무경유·인용 강제) 유지.

### Phase D — 추출 정밀화 — 커밋 `59b1bfe`
- `_note_body_text`/`_annotate_notes`: 본문-존재 필터(헤더 오탐 드롭·30자 하한) + 노트별 `detected_unit`.
- 드라이런: 12셀 노트 수 불변(legit 손실 0). 단위 메타 추가.

### Phase A·B·C·E — 커밋 `c047424`
- **A(본문 청크 인덱싱)**: `_chunk_text`/`_note_chunks` — 노트 페이지범위를 page-정확 청크(≈450자·overlap80·노트당16)로 분할, 청크별 임베딩+토큰. `index.json schema=2`. 구 인덱스(chunks 부재)는 제목-only 폴백.
- **B(형태소 토크나이저)**: `tokenize_korean` 을 kiwipiepy(싱글톤·오프라인) 내용형태소 추출로 교체(조사 분리). 미설치 시 정규식 폴백. 인덱싱·질의 동일 사용.
- **C(청크-aware 검색)**: `match_query_in_notes`(compare/coverage 공유)·`notes_rag.retrieve` 가 **제목+청크 cosine 최댓값** + BM25 제목+청크 합본 코퍼스. `match_page`=최고 유닛 실제 페이지 → RAG/메모 인용 페이지 정밀화(`build_prompt` 태그도 match_page 우선). 엔드포인트는 청크 본문 우선·부재 시 페이지추출 폴백.
- **E(동의어 확장)**: `synonyms.py` — 회사변형 회계용어(대손충당금↔대손준비금 등) BM25/lexical 질의 확장. 한글 경계 인식(`리스`가 `리스크`에 오확장 안 됨). 임베딩은 원질의 유지(정밀도 보존).

### 재인덱싱·검증
- 12셀 재인덱싱 → schema=2, 셀당 ~340~510 청크, kiwi 형태소 토큰 확인.
- 청크 검색 직접 테스트: "기대신용손실 측정 가정" → 제목에 없는 주석17 본문 청크(p212) 매칭.
- 라이브 RAG: mode=rag, 본문매칭 출처, **match_page(212)≠page_start(211)** 정밀 인용.
- 신규 테스트 `test_notes_rag_chunks.py`(3)·`test_synonyms.py`(4). **pytest 39 passed**.
- 동의어 테스트가 `리스`→`리스크` 오확장 버그를 잡아 한글 경계 매칭으로 수정.

### 결정 사항
- 리랭킹은 **점수 융합 only**(cross-encoder 미도입 — exe 용량·폐쇄망 영향 0). 후속 옵션으로 문서화.
- compare/coverage 는 단일 best 유지(UX), top-K 정밀화는 RAG retrieve 에 적용.
- 임계(MIN_MATCH/HIGH_CONF)는 유지 — 청크로 cosine 최댓값 상승해 재현율↑, lexical_hit 게이트로 보강.

### 미완료/다음 작업
- (선택) **exe 재빌드** — 개선된 검색·신규 UI·kiwipiepy·schema2 반영. CPU-only torch 전환 시 6.1GB→~2.5GB.
- (선택) 인덱스 용량(청크 임베딩) float16 저장으로 축소, cross-encoder 리랭커.

## 2026-06-02 (저녁) — UI 수정 + 라이브러리 자료 추가

### RAG 탭 분리·출처 개선 — 커밋 `4c18ee4`·`86fa437`
- 주석 자연어 질의(RAG)를 **독립 탭** '🤖 주석 자연어 질의 (AI 보조)'로 분리(비교조회↔재무제표 사이). '비교 조회'→'주석 비교조회' 개명.
- `showTab` 을 `data-tab` 기반으로 교체 — '비교'/'재무제표 비교' 동시 active 버그 수정.
- RAG 출처 칩: 페이지·% + **매칭 본문 샘플(≤100자, 매칭 용어만 볼드)**. `/api/notes/rag` 가 snippet·terms 동봉.

### 라이브러리 신규 분기/연간 자료 추가 — 커밋 `59b48ee` (백엔드+프론트)
- **백엔드**: `fs_compare.cmp_col(sj_div, period)` — FY 손익은 전기 **연간**(frmtrm) 비교(분기는 frmtrm_q 유지) → 사업보고서 손익 증감 N/A 해소. `app.period_sort_key`(Q1<Q2<Q3<FY) → available_periods/matrix 정렬.
- **프론트(UI)**: 라이브러리 탭 '➕ 분기/연간 자료 추가' **인라인 패널**(회사/4사일괄·연도·유형·본문PDF→DART 수집). 하드코딩 기간 드롭다운 제거 → **연도+보고서유형 동적 셀렉터**(미래 분기/연간 무제한). `EXTRA_PERIODS`(localStorage)로 **빈 컬럼 즉시 노출**, **빈셀 클릭=패널 프리필+1클릭 수집**. 매트릭스 FY 정렬·'(연간)' 라벨·음영.
- **검증**: preview 에서 동적 연도(2020~2026)·유형(1분기/반기/3분기/사업보고서)·4사일괄·빈셀 프리필·FY 정렬(2025Q3<2025FY<2026Q1) 확인. **pytest 40 passed**. 외부 https 0.
- 결정: 백엔드는 이미 FY 수집 지원(`_SUFFIX_TO_REPRT`)이라 UI 노출만 추가. 실수집은 DART 키 가동 시.

## 2026-06-02 (밤) — Claude 큐레이션 인덱싱 하베스트 (B안, /arch 워크플로)

ollama refine 대비 **주석 분할/제목/연결별도/주제** 품질을 Claude로 끌어올리는 오프라인 큐레이션 도입.
에이전트가 notes 초안 작성 → 스크립트가 검증·diff·apply(로컬 임베딩 재계산). 데모 PC 무영향(개발 PC 전용).

### 커밋 `07ee2d0` (Architect→Builder→Reviewer)
- **리팩터**: `index_entry` 의 임베딩·청크·index.json(schema2)·카탈로그 기록을 `embed_and_write_index()` 로 분리. INDEX_STATUS 는 progress_cb 콜백으로 분리(코어 순수). 런타임·큐레이션이 **동일 코어** → 스키마 드리프트 0. 신한 2026Q1 재인덱싱 스냅샷 동일(notes 46/chunks 340) 검증.
- **신규 `scripts/curate_index_claude.py`**(개발 PC 전용, exe 번들 제외): `validate`/`diff`/`apply`. 안전경계 — 제목 verbatim(PDF 텍스트 존재)·숫자성 제목 거부·페이지경계는 **hard violation**(차단), 단조·중복은 **warning**(비차단). `apply` 는 `--yes` + `index_heuristic.bak.json` 백업. 네트워크/외부API 0.
- **`curate_validate`** 순수 함수((violations,warnings) 분리) + `tests/test_curate_validate.py`(10). diff 키=(fs_div,title).
- 리뷰: 리팩터 byte-identical PASS·안전경계 PASS, SHOULD-FIX 2건(warning분리·diff키) 반영. **pytest 50 passed**.

### 파일럿 큐레이션 완료 — 신한 2026Q1 (데이터: storage, gitignore)
- **휴리스틱 결함 발견**: 연결 note1 "회사의 개요"가 p104–172(68p)로 비대 → 누락 주석 2·3·**4(금융상품 위험관리, 4-1/4-2/4-3, p110–172 62p)** 흡수. note32 p278–498(220p) 비대. 별도도 동일(note3 p283–311이 별도 note4 흡수, note25 p340–498). 13/14/18/20은 회사가 번호 건너뜀(실재 X).
- **큐레이션**: 에이전트가 PDF 헤더 후보+갭 정밀스캔으로 실구조 복원 → notes_curated.json(연결30+별도24=54, vs 휴리스틱 46). `validate`(verbatim·숫자·경계 위반 0)→`diff`(추가8/변경4/삭제0)→`apply`(index_heuristic.bak.json 백업 후 임베딩·청크 재계산).
- **검증**: "금융상품 신용위험 관리/공정가치 서열체계" 질의 → 이전엔 **전무**하던 note4 가 top3(공정가치 87%·범주별 81%·기본 78%)로 정확 매칭, match_page 정밀. pytest 50 passed.
- 롤백: `index_heuristic.bak.json` 보관. 데모 배포 시 큐레이션된 storage 반영.

### 큐레이션 4사×3분기 전수 완료 — 커밋 `0835fc5` (auto 모드)
- **보편적 결함 진단**: 12셀 전부 마지막 주석이 150~384p 비대(말미 부록·배당·감사 흡수). 신한은 추가로 정책·금융위험관리(4-x) 누락(접미 부재).
- **`auto` 모드** 신설: 기존 notes + `N-x` 서브헤더 + 비접미 정책노트를 **주석 영역 한정** 수집 → 페이지순 next-1 경계 재계산(마지막은 후속 섹션[배당 등] 앞 컷). 재무제표 본문 제목(재무상태표/이익잉여금처분계산서 등) 제외, 병합 서브헤더 컷.
- **전수 적용**(auto→validate→diff→apply): 비대 범위 **150~384p → 24~48p** 해소. 신한 위험관리(연결/별도 4-x)·보험·자본 서브노트, 우리/하나 다수 서브노트 복구. 각 셀 `index_heuristic.bak.json` 백업.
- **검증**: 12셀 max범위 24~48p·schema=2·bak 전수 확보. pytest **50 passed**. 하나 2026Q1 RAG 검색(위험관리 등) 정상. 데이터는 storage(gitignore) — 배포 시 큐레이션 storage 반영.

### 미완료/다음(선택)
- exe 재빌드(CPU-only torch, 6.1GB→~2.5GB) + 큐레이션 storage 동봉.
- 하나 2026Q1 일부 서브노트 제목 다듬기(콤마/긴 제목), 청크 페이지 상한(현 10p) 상향 검토.

## 2026-06-02 (세션 종합 마감)

### 이번 세션 산출물 총정리 (main 최신 `ce563ac` 기준, 전부 push)
- **C 코드정리**: dormant wordmap 제거(`fb54352`), `safety.py` 마스킹·provenance 중앙화(`7e61f27`).
- **B 빌드**: `paths.py` 경로중앙화(`41cb240`), Playwright E2E(`b84220d`), PyInstaller onedir 풀번들+`build.ps1`+`run_server.py`(`1499967`). 산출물 5.95GB(검증 OK).
- **검색 정확도**: Phase D 추출정밀화(`59b1bfe`) + Phase A·B·C·E 본문청크·kiwi·청크검색·동의어(`c047424`).
- **RAG UI**: 전용 탭 분리·"주석 비교조회" 개명(`4c18ee4`), 출처 매칭 본문 볼드 샘플 ≤100자(`86fa437`).
- **라이브러리**: 분기/연간 자료추가 UI(동적 셀렉터·인라인 패널·빈셀 수집)+FY 비교컬럼(`59b48ee`).
- **Claude 큐레이션**: `embed_and_write_index` 코어추출+`curate_index_claude.py`(`07ee2d0`), `auto` 모드로 4사×3분기 전수(`0835fc5`).

### 현재 상태
- **pytest 50 passed**, 외부 https 0, 서버 :8021 단일 가동(ollama·로컬임베딩·BM25·lib 12).
- 12셀 schema=2 + Claude 큐레이션(비대 24~48p·누락주석 복구·셀당 .bak).
- 안전경계 전부 유지(숫자 AI무경유·인용강제·외부CDN0·키마스킹).

### 인수인계
- `handoff/prism-fs_RAG개선_큐레이션인덱싱_20260602_2040.md` 작성(세션 전체 인수인계, gitignore).

### 권장 다음 작업
1. **배포 exe 재빌드**(권장·미착수): `build.ps1` 재실행 → 개선 검색·신규 UI·큐레이션 storage·kiwipiepy 반영 + CPU-only torch 전환(6.1GB→~2.5GB).
2. 큐레이션 품질 보강(하나 2026Q1 제목·청크 페이지 상한).

## 2026-06-02 (이어서) — 큐레이션 품질 보강 (제목 정리 + 청크 커버리지)

- **작업 내용**:
  - **청크 커버리지 개선(페이지 분산)**: 측정 결과 긴 노트 커버리지 병목은 페이지 상한(10p)이 아니라 **노트당 청크 수 상한(16)** 임을 확인(75노트 중 17노트가 16청크 도달, 앞 3~6p에서 예산 소진). `_note_chunks` 에 **페이지당 청크 상한(`MAX_CHUNKS_PER_PAGE=3`)** 도입 → 긴 노트가 앞 페이지에서 예산을 독식하지 않고 전 범위에 분산. 상한 동반 상향: `MAX_CHUNKS_PER_NOTE 16→40`, `CHUNK_SCAN_PAGE_CAP 10→20`.
  - **하나 2026Q1 제목 정리(병합 제목 분리·서브표 정리)**: 휴리스틱이 인접 헤더를 ', NN. ' 로 병합한 제목(예: `"자본금 및 자본잉여금, 32. 자본조정, 33...., 34. 이익잉여금"`)을 `_split_merged_title` 로 **개별 노트로 분해**(사채·자본조정·기타포괄손익누계액·이익잉여금·기타비용 등 복구). PDF N-x 서브헤더 `"부모, 자식"` 형식(예: `"기타부채, 리스부채"`)은 `_clean_title` 로 **자식 라벨만**(리스부채·충당부채·우발ㆍ약정사항) 남겨 검색성↑·중복 제거. step1(기존 인덱스)·step2(N-x 재수집) 동일 경로 통일로 dedup.
- **변경 파일**:
  - `src/app.py`: `_note_chunks` 페이지 분산 로직 + 청크 상수 3종.
  - `scripts/curate_index_claude.py`: `_split_merged_title`·`_clean_title`·`_resolve_subnote_page` 추가, `_auto_draft` step1 분해 통합, step2 N-x 제목 정리 일관 적용.
- **결정 사항**:
  - 60자 절단 잔재(예: `"...21. 이"`)는 1글자 꼬리 → `len(_norm)>=2` 가드로 드롭(가비지 제목 방지).
  - 페이지당 3청크 = breadth 우선(긴 주석 본문은 페이지 상단 서술이 검색 핵심). 인덱스 +20%(11.5→13.8MB)는 폐쇄망 PC 허용 범위.
  - 재인덱싱은 하나 2026Q1 선검증 후 **12셀 전수 확대**(사용자 선택). auto 초안 11셀 일괄 검증(통과·잔재0) → apply.
- **검증**:
  - 하나 2026Q1 apply(26초): 75→**85 노트**(연결52/별도33), 쉼표·번호 잔재 **0건**, 검증 통과(경고 5·비차단), `index_heuristic.bak.json` 백업.
  - 커버리지: 위험관리 4p→**14p**, 특수관계자(41p) 6p→**15p**, 일반사항 4p→14p. 페이지 상한 내 노트는 전체 커버(파생상품 12/12·관계기업 10/10·보험계약 13/13).
  - 검색: 분리 노트 `리스부채` score 1.000 적중, 특수관계자 41p 노트 검색. `_load_index` 디스크 매회 로드 → 서버 재기동 불필요.
  - **12셀 전수 적용 후 재검증**: schema=2·잔재 제목 0·긴 노트 커버리지 평균 **85~92%**(이전 ~10~40%)·index 9.6~13.8MB. **pytest 50 passed**(전수 적용 후 재실행). 안전경계 불변(숫자 AI무경유·verbatim·인용강제·외부CDN0·키마스킹).
- **미완료/다음 작업**: 배포 exe 재빌드(개선 검색·신규 UI·전수 큐레이션 storage 반영 + CPU-only torch).
- **이슈/특이사항**: 큐레이션 데이터(index.json)는 storage(gitignore) — 코드(app/scripts)만 커밋 대상. 각 셀 apply 시 `.bak` 는 직전 큐레이션본으로 갱신(롤백=직전 양호본).

## 2026-06-02 (이어서) — 주석 XBRL 상세태깅 진단 (DART detail-tagging audit)

- **배경/목표**: DART 주석 상세태깅 범위가 공시·기간별로 편차가 큼. 프로젝트는 이미 4사 전 셀의 **DART 원본 XBRL 일습**(instance·xsd·linkbase)을 storage 에 보존하나 1차 재무제표만 활용 → 주석이 XBRL로 **어디까지 태깅**됐는지를 오프라인·결정론으로 정량화해 UI 노출. (Plan mode 승인 플랜: `~/.claude/plans/dart-detail-pure-breeze.md`.)
- **데이터 모델(탐색 확정)**: `.xsd <link:roleType>` = 상세태깅 메뉴(`[DXNNNNNN] N.제목`), 주석=DX8 계열·본문=DX2/3/5/6. **fs_div 규칙: DX 끝자리 0=연결, 5=별도**(DX835100↔DX835105). `_pre.xml presentationLink` = 실제 사용 롤(선언−사용 차집합=미태깅). instance fact 는 role 미보유 → presentation loc(concept) 로 귀속.
- **아키텍처**: 12MB instance 파싱을 요청 경로에서 분리 — 개발PC 사전계산 → 셀별 `xbrl_tagging.json`(storage, gitignore) → 런타임 엔드포인트·UI 는 JSON 만 읽음(파싱 0). `fs_structured.json` 과 동일 패턴.
- **작업 내용 / 변경 파일**:
  - 신규 `src/xbrl_tagging.py`: ElementTree 스트리밍 순수 엔진. L1(roleType 선언 vs presentation 사용)·L2(concept→role 역인덱스 + instance fact 집계, **start-ns 로 QName prefix 복원**, textblock 분리)·L3(PDF index.json↔XBRL 롤 fs_div 한정 결정론 매핑, 형제 세부롤 거짓갭 제거). 표준 라이브러리만.
  - 신규 `scripts/build_xbrl_tagging.py`: curate 미러 CLI — `build/build-all/matrix`. 개발PC 전용·exe 번들 제외·네트워크/LLM 0.
  - `src/app.py`: `GET /api/xbrl-tagging`·`/api/xbrl-tagging/matrix`(경량 JSON 로드, `_load_xbrl_tagging`).
  - `src/static/index.html`: 신규 탭 "🏷️ 주석 XBRL 태깅" — A.4사×분기 횡단 매트릭스(클릭→상세) + B.셀 상세(L1/L2/L3 카드·미태깅 칩·롤별 충실도). 외부 CDN 0.
  - 신규 `tests/test_xbrl_tagging.py`(8): definition 파싱·fs_div 규칙·href→QName·L1 차집합·L3 매핑.
- **검증(라이브 :8021 + preview)**:
  - 11/12 셀 진단 생성(하나 2025Q2 = XBRL 미수집, status 로 정상 처리). 결정론(동일입력 동일출력).
  - **편차 재현**: facts 신한 7170/KB 12626/하나 12635/우리 8430, textblock 신한0·KB19·하나17·우리0. **기간편차**: 신한 facts 8170→7991→7170, 우리 textblock 25→32→0.
  - **L3**: 신한 47/61(연결 28/35·별도 19/26). note4↔DX835100·note6↔DX804000·note9↔DX819000 강매칭. **pdf_only**=회사의개요·작성기준·회계정책·종속기업변동 등 서술형 미태깅 정확 포착. **xbrl_only**=병합주석(13.14, 9.10)만.
  - preview 브라우저: 매트릭스 12셀 렌더·셀 클릭 상세·콘솔 에러 0. **pytest 50→58 passed**, 외부CDN 0, 안전경계 불변(숫자 AI 무경유·결정론).
- **결정 사항**: 1차 dimension-naive(차원 무시·concept 단위 집계, `_def.xml` hypercube 후속). UI 통합 but 파싱은 사전계산으로 핫패스 분리. 진단 JSON=storage(gitignore), 코드만 커밋.
- **미완료/다음 작업**: (선택) 차원별 fact 귀속, 배포 exe 재빌드 시 신규 탭·엔진 포함 + xbrl_tagging.json 동봉.
- **이슈/특이사항**: 하나 2025Q2 xbrl/ 빈 디렉토리(반기 [정정] XBRL 미수집) — 데이터 가용성 갭. 서버는 app.py 변경 반영 위해 재기동 필요(preview 가 :8021 소유).

## 2026-06-04 — XBRL 태깅 L2 차원 인지(dimension-aware) 정밀화

- **배경**: dimension-naive L2 는 개념 단위로만 fact 를 세고 그 개념이 등장하는 모든 롤에 전체 fact 를 중복 가산 → 신한 2026Q1 롤별 합 15,395 vs 실제 7,170(**2.1배 과다귀속**, concept 726/1733 다중롤 공유). L1 범위·L3 매칭은 영향 없고 L2 절대값만 부정확.
- **작업 내용 / 변경 파일** (`src/xbrl_tagging.py`):
  - `parse_def_hypercubes(_def.xml)`: XDT 구조(all=LineItems→hypercube, hypercube-dimension→axis, dimension-domain→domain, domain-member 트리) 파싱 → 롤별 `{primary_items, axes}`. 도메인(차원) 측 노드는 primary 에서 제외.
  - `parse_context_dims(instance)`: context id → 명시 차원 축(explicitMember `dimension` 속성 QName, ET 미확장이라 prefix:Local 로 바로 일치).
  - `count_facts_dim`: fact 를 `(concept, 축집합)` 버킷으로 집계.
  - `route_fact`: 개념이 primary_item 인 롤 중 **축집합 ⊆ 롤 axes 가장 타이트한 1개**로 단일 귀속(결정론). 본문(재무제표) 개념은 미귀속.
  - `build_l2_dim`: 단일 귀속 합산(중복 제거) + 롤별 `axes_count`(차원수) + totals `attributed/unattributed/max_axes`. `_def.xml` 부재 시 dimension-naive 폴백.
  - 엔드포인트 matrix 에 `max_axes` 추가. UI: 매트릭스 "최대 차원수" 열, 셀 카드 "fact(주석 귀속/본문)·최대 차원수·차원 인지 주석", 롤별 표 "차원(축)수" 열.
- **검증(라이브 :8021 + preview)**:
  - 신한 2026Q1: 롤별 numeric 합 15,395→**5,683 = attributed**(과다귀속 해소), attributed+unattributed=7,170=total 단일귀속 확인. **max_axes=7**(신용위험표), 롤별 축수=공정가치 11·파생 9·특수관계자 3.
  - 12셀 재생성 ~10초(_def 최대 11MB 안정). UI: 매트릭스 차원수 열·셀 카드·롤표 차원열 렌더, 콘솔 에러 0.
  - 단위테스트 +3(route_fact 단독/타이트축/단일귀속). **pytest 61 passed**(라이브 포함). 안전경계 불변(순수 집계·네트워크/AI 0).
- **결정 사항**: 라우팅은 축-부분집합 기준(member 일치까지는 미요구 — 이 분류체계에선 축으로 표 식별 충분). 미귀속분(신한 1,487)=본문 재무제표 개념(주석 primary item 아님)으로 정상.

## 2026-06-04 — 검색 정확도: note_kind 인지 가중·임계(B-1/B-2)

- **배경**: 서술형/정책 주석 검색 정확도 개선 가이드 중 1순위. `note_kind`(note_filters.py) 실측: **"주기"=회계정책·작성기준·일반사항·회사개요 등 prose 정책주석**(키워드 빈약·XBRL 미태깅 pdf_only로 확인)이 약한 클래스, **"서술형"=파생상품·대출채권 등 항목공시**(표·키워드 신호 보유, 이미 잘 동작 — "파생금융상품 평가손익"→파생상품 top 실측).
- **경로 사실**: `match_query_in_notes`(비교조회·커버리지)=하이브리드 0.7cos+0.3bm+동의어+keep 임계 → B-1/B-2 적용 지점. `notes_rag.retrieve`(자연어질의 탭)=cosine-only(BM25·임계 없음) → 서술형엔 이미 임베딩-순수라 B-1/B-2 무관.
- **작업 내용 / 변경 파일** (`src/app.py`):
  - **B-1**(note_kind 가중): 주기=`0.85cos/0.15bm`(임베딩 비중↑), 서술형·기타=현행 `0.7/0.3`. 노트별 가중 벡터로 `final` 산출([app.py match_query_in_notes]).
  - **B-2**(note_kind 임계): 최종 hit 가 주기면 채택 하한 `POLICY_MIN_MATCH=0.40`(완화), 서술형은 `MIN_MATCH_SCORE=0.45`(현행). keep 판정 분기.
  - 상수 5종 env 외부화(`COS_W_DEFAULT/BM_W_DEFAULT/COS_W_POLICY/BM_W_POLICY/POLICY_MIN_MATCH`). 반환 dict에 `note_kind`·`min_match` 추가(투명성).
  - `tests/test_match_kind.py`(2): USE_BM25 off로 score=cosine 통제 → 0.42 경계에서 주기 keep=True·서술형 keep=False(무회귀) 결정론 검증.
- **검증**: 정책 질의("중요한 회계정책…","회계추정 및 판단")→주기 채택·min0.4·high conf. **파생상품 무회귀**(서술형·min0.45·score1.000), "파생금융상품 평가손익"→파생상품 keep. 커버리지 탭 UI 정상·콘솔 에러 0. **pytest 63 passed**(라이브 포함). 안전경계 불변(텍스트·결정론, 동의어는 BM25에만·임베딩 원질의).
- **미적용(후속 가이드)**: A-1 동의어/약어 확장(ECL·FVOCI 등), notes_rag.retrieve 에 하이브리드+동의어 도입(현 cosine-only), C-1 골든셋 측정 루프, B-3 서술형 청킹, B-4 재랭킹.

## 2026-06-04 — 검색 후속: 동의어/약어(A-1)·RAG 하이브리드(#2)·골든셋 평가(C-1)

- **A-1 동의어/약어 확장** (`src/synonyms.py`): 영문 약어→한글 정식명 그룹 추가 — `ECL`(대손충당금 군에 합류)·`FVOCI`→기타포괄손익공정가치측정·`FVTPL/FVPL`→당기손익공정가치측정·`EIR`→유효이자율·`AC측정`→상각후원가측정. `_has_term` 경계를 한글+영숫자로 일반화 + `re.IGNORECASE`(약어 대소문자 무시). 보수 원칙 유지. 실측: `ECLS`/`리스크관리` 오확장 없음, `fvtpl`(소문자) 정상 확장.
- **#2 RAG 하이브리드+동의어** (`src/notes_rag.py`, `src/app.py`): 자연어질의 경로(`notes_rag.retrieve`)가 cosine-only였음 → **BM25(제목+청크 토큰)+동의어 확장+note_kind 가중**을 도입. 순환 import 회피 위해 `tokenize/expand/bm25_cls`를 호출부(app)에서 **주입**(미주입 시 cosine-only 하위호환 — 기존 테스트 유지). `/api/notes/rag`·compare-memo 두 호출부에 주입. 실측: "FVOCI 평가손익"→기타포괄손익 노트 0.769→0.838 상승.
- **C-1 골든셋 평가 루프** (`scripts/eval_search.py`, `scripts/golden_search.json`): 14개 대표 질의(정책/동의어/약어/항목) → 가동 서버 `/api/notes/rag` 질의해 hit@1/3/5·MRR 카테고리별 집계(urllib, 외부 호출 0). **결과: 전체 hit@3=1.0·hit@5=1.0·MRR=0.964**, 약어(FVOCI)·동의어(파생금융상품) rank1 적중. 정책 hit@1=0.667(유사 정책주석 혼동, hit@3=1.0). 향후 가중·임계·동의어 튜닝의 근거 베이스라인.
- **검증**: pytest **63 passed**(라이브 포함), 안전경계 불변(텍스트·결정론, 동의어는 BM25/lexical에만·임베딩 원질의). 데이터(index/golden 결과 JSON)는 산출물 — 코드만 커밋.
- **다음 후보**: 정책주석 변별(유사 정책 혼동) 개선, 골든셋을 회계팀 확정셋으로 확대, B-3 서술형 청킹·B-4 재랭킹.

## 2026-06-04 — 동의어 코퍼스 조사·평가셋 반영도 측정(P2~P4) + 회계팀 검수표

- **조사**: 2026Q1 4사 주석 제목 실측 → **회사 간 동일개념 표기 변형** 다수 발굴(회사정보: 회사의 개요/일반사항/지배기업의 개요, 위험관리: 금융상품 위험 관리/금융위험의 관리/위험관리, 부문: 영업부문/부문별공시, 순이자손익/순이자이익, 법인세/법인세비용/이연법인세, 추정 판단/가정).
- **P2 사전 확장** (`src/synonyms.py`): 위 안전 그룹 7종 추가(net 항목 손익↔이익은 순이자·순수수료 한정). 구기준(IAS39↔IFRS9)·고위험은 미적용→검수표로.
- **P3 골든셋** (`scripts/golden_search.json`): 14→23문항. 카테고리 `동의어-교차`(7)·`오확장`(2) 신설.
- **P4 eval 프로브** (`scripts/eval_search.py`): `--probe` — synonyms 그룹에서 교차변형 테스트 **자동 생성**(한 변형 질의→코퍼스 실재 다른 변형 노트 top-k 적중). `--generate=false`(엔드포인트 신규 파라미터)로 LLM 생성 생략 고속 평가. 동의어 커버리지·교차변형 hit@k 리포트.
- **검수표** (`doc/회계동의어_검수표_20260604.md`): A.반영분(코퍼스 실측 동일개념) / B.검수대기(구기준·차입·리스·손상 등 위험) / C.병합금지(영업수익↔손익 등). 회계팀 `검수결과` 기입란 포함.
- **엔드포인트**: `/api/notes/rag` 에 `generate: bool=True` 추가 — false 시 검색만(인용강제 유지). 평가/디버깅 고속 경로.
- **측정 결과**: 골든 23문항 hit@3=1.0·MRR 0.949, **동의어-교차 hit@3=1.0**(이연법인세만 rank3), **오확장 hit@1=1.0**(리스크관리→위험관리·영업손익 정상). 프로브: **사전 26그룹 중 20 테스트가능(커버리지 0.769)·교차변형 hit@5=0.962**. 미테스트 6=제목 미등장 그룹(약어 텍스트형·영업권·당기순이익) → 본문 프로브는 후속.
- **검증**: pytest **63 passed**, 안전경계 불변(동의어 BM25/lexical 전용·임베딩 원질의). 데이터(golden 결과)는 산출물 — 코드/문서만 커밋.

## 2026-06-04 — 검색정확도 후속 일괄(카테고리 1)

- **동의어 데이터 분리**(`src/synonyms_data.json` + `synonyms.py` 로더): _SYNONYM_GROUPS를 json으로 외부화(storage 오버라이드>번들 폴백). 비개발자 편집·검수표 반영 용이. 29그룹.
- **검수표 B 안전분 반영**: IAS39↔IFRS9 구기준명(매도가능→FVOCI·만기보유→AC·단기매매→FVTPL)·자기주식↔자사주·신종자본증권↔영구채·현금및현금성자산·외화환산·리스↔사용권자산·사채↔차입부채 추가. 손상↔평가손실(고위험)은 검수대기 유지.
- **정책주석 변별(B-1 보완)**: `notes_rag.retrieve`에 **IDF 가중 제목 토큰 일치 보너스**(`title_bonus=0.15`) — 질의어가 제목에 직접 등장 시 가산하되 희소(변별력 높은) 토큰 우대(제네릭 금융/자산 약화). 회계정책 vs 회계추정 등 유사 제목 변별.
- **본문 프로브(eval)**: `eval_search.py --probe` 의 in_corpus 판정·hit 판정을 **제목+본문 청크 정규화 매칭**(하이픈/공백/중점 제거)으로 확장 → 약어 텍스트형(FVOCI 등) 그룹도 테스트.
- **골든 확대**: FVOCI 정답에 공정가치 포함(밀접 관련 인정), 별도 케이스 2건 추가(25문항).
- **측정 결과**: 골든 25문항 **hit@1=1.0·MRR=1.0**(정책 0.667→1.0, FVOCI·별도 포함 전 카테고리 완벽). 프로브 **동의어 커버리지 0.769→0.966**(28/29 테스트가능)·교차변형 hit@5 0.937.
- **검증**: pytest **63 passed**. 안전경계 불변(IDF·제목보너스 모두 결정론·텍스트, 임베딩 질의 원문 유지).
- **이번 배치 미적용(비용·블로커)**: A-2 kiwi 사용자사전·B-3 서술형 청킹 = **전 셀 재인덱싱 필요**(코퍼스 토큰 일관성)로 보류, B-4 재랭킹 = **오프라인 cross-encoder 모델 번들 결정 필요**로 보류.

## 2026-06-04 — A-2/B-3 시도 후 롤백 (실측 회귀)

- **시도**: A-2(kiwi 사용자사전 — 도메인 합성어 20개 단일 토큰 고정) + B-3(주기 prose 청크 700자) 구현 → 12셀 재인덱싱(index.prebak_a2b3 백업).
- **측정**: 재인덱싱+서버 재기동 후 골든 **hit@1 1.0→0.88·MRR 1.0→0.933**, **동의어-교차 1.0→0.571**, 프로브 교차변형 hit@5 0.937→0.911. → **회귀**.
- **원인**: 사용자사전이 합성어를 단일 토큰으로 묶어 **교차변형 BM25 중첩(공유 sub-토큰)을 약화**(예: 변형 질의가 '기타포괄손익' 통째 토큰과만 매칭). **세밀 형태소 분할이 이 코퍼스의 교차변형 재현율에 더 유리**함이 입증됨.
- **조치**: **A-2·B-3 전면 롤백** — index.prebak 복원·제거(12셀), app.py 코드 되돌림(주석으로 결정 근거 명시). 재측정 골든 **hit@1=1.0·MRR=1.0 복귀**. pytest 63 passed.
- **결론**: C-1 평가 루프가 회귀를 사전 차단(반영 전 측정→롤백). A-2/B-3은 본 코퍼스엔 **부적합으로 종결**(재시도 비권장). 남은 후속은 B-4(모델 필요)·골든셋 회계팀 확정셋 확대.
