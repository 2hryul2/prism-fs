# 개발노트 — 검토보고서(review) 문서축 추가

## 2026-06-05 14:5x 작업 내역

- **작업 내용**: 검토보고서(분기연결검토보고서, review)를 1급 선택 차원(doc_type)으로 추가. 라이브러리·비교조회·자연어질의(RAG)·커버리지/주제맵/공시갭/비교메모에서 분기보고서(report)와 검토보고서를 모두 선택·비교·질의 가능하도록 확장.
  - **Step R1 (백엔드)**: `src/app.py` 5개 GET 라우트(account-refs·topic-map·compare-memo·rag·terms-suggest)에 `doc_type: Literal["report","review"]="report"` 파라미터 추가, `_load_index(..., doc_type)` 전파. 하위호환 100%.
  - **Step R2 (프론트)**: 선택키 3축화(`회사/기간#doc_type#fs_div`), `selKey`/`parseSelKey` 4인자화·전수 호출처 갱신. 라이브러리 셀에 검토보고서(연결) pill 추가(report 연결/별도 + review 연결, review 별도는 렌더 안 함). `docIndexed`/`docFsCount`/`DOC_ICON`/`DOC_SHORT` 헬퍼. 비교 targets·패널 라벨·`renderPdfRange(doc_type)`·`selectCandidate(m.doc_type)`·RAG `#rag-doctype` 토글 배선.
  - **Step R3 (프론트)**: 커버리지/인사이트 탭 문서축(`INSIGHT_DOCTYPE` + `#cov-doctype`, review 선택 시 fs_div=연결 강제·cov-fsdiv disable). coverage·gap·topic-map·compare-memo 호출 및 PDF 딥링크에 doc_type 전파. doCompare stale 경고문(별도 미추출) → review+별도 한정·문구 수정. RAG·비교메모 출처 PDF 딥링크 doc_type 반영(Reviewer SHOULD-FIX 2건).
- **변경 파일**:
  - `src/app.py` (백엔드 5개 라우트 doc_type)
  - `src/static/index.html` (선택키·라이브러리·비교·RAG·커버리지·딥링크)
  - 신규 문서: `ideation/ideation_검토보고서라이브러리추가_20260605_1446.md`, `spec_20260605_1449.md`
- **결정 사항**:
  - 검토보고서는 **주석만 보유**(XBRL·구조화 재무제표 없음) → **XBRL 태깅 탭·재무제표 비교(fs) 탭은 report 고정 유지**(데이터 부재). Known Gap.
  - 분기보고서·검토보고서 **둘 다 유지**(교체 아님). 프리셋은 report 유지(검토 자동선택 금지).
  - review 인덱스는 연결 단일 → review+별도는 빈결과 정상(오류 아님).
- **미완료/다음 작업**:
  - 브라우저 수동 UI 확인(셀 pill 3개 레이아웃·토글 동작) — owner 확인 권장.
  - owner 커밋 승인 대기. 승인 시 주제별 분리 커밋(R1 백엔드 / R2·R3 프론트).
  - 폐쇄망 배포 시 VERSION 0.1.0→0.2.0 재빌드(`build.ps1`).
- **이슈/특이사항**:
  - 라이브 검증: 신한 2026Q1 report연결(주석20 자본 p219) ↔ review연결(주석19 보험금융손익 p103) 결과 상이 확인. review+별도 matches=0(정상). pytest 76 passed, 외부 https 0건, node --check OK.
  - 서버 :8021 재기동 완료(PID 15980) — R1~R3 반영본 가동 중.
