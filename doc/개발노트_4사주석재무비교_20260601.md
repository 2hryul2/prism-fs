# 개발노트 — prism-fs (4대 지주 주석·재무제표 비교 대시보드)

## 2026-06-01 09:37 작업 내역 (프로젝트 생성 — 기획 + UI 프로토타입)

- **작업 내용**:
  - 신규 독립 프로젝트 `prism-fs` 스캐폴드 생성(CLAUDE.md 표준 구조).
  - 아이데이션 문서(구현방안 3안·제약/극복·언어선정·분석기능 카탈로그 P1/P2) 작성.
  - EARS 스펙 15개(Ubiquitous 7·Event 7·Unwanted 5·State 2·Optional 2) 작성.
  - 단일 HTML 대시보드 프로토타입 구현(오프라인·외부 CDN 0, 실제 fs_structured.json 슬라이스 임베드).
  - 4탭: 라이브러리 매트릭스 / 재무제표 비교(핵심) / 주석 검색(데모) / 커버리지(데모).
  - 재무제표 비교 4서브뷰: ①시계열 증감 ②연결vs별도 ③4사 벤치마킹 ④안전 구조비율. 전부 provenance 팝오버.

- **변경 파일**:
  - `VERSION`(0.1.0), `.env.example`, `.gitignore`, `requirements.txt`, `README.md`
  - `ideation/ideation_4사주석재무비교_20260601_0937.md`
  - `spec_20260601_0937.md`
  - `src/static/index.html` (프로토타입)
  - `doc/개발노트_4사주석재무비교_20260601.md`
  - (참조 프로젝트) `D:/SOURCE/prism/.claude/launch.json` 에 `prism-fs-proto`(static :8020) 추가

- **결정 사항**:
  - 프로젝트 형태=독립 신규(사용자), 위치=`D:\SOURCE\prism-fs`, 첫 산출물=기획+프로토타입.
  - 스택: 백엔드 Python3.11+FastAPI(다음 세션 prism 이식), 프론트 Vanilla JS+순수 CSS.
  - 폐쇄망: 외부 CDN 0(Tailwind/pdf.js CDN 미사용) → vendored 전략.
  - 비교 컬럼 sj_div 자동선택: BS=frmtrm(전기말), 손익(CIS)=frmtrm_q(전기동기). bfefrmtrm 추론 금지.
  - 연결/별도 비교는 동일 fs_div끼리만, 한쪽만 존재 시 "대응 없음" 플래그(차감 안 함).

- **검증 (라이브)**:
  - 외부 https 호스트 참조 **0건**(w3.org/json-schema 외).
  - 임베드 수치 = 원본 fs_structured.json **바이트 일치**(자산총계 816,718,546,000,000 등 스팟체크 OK).
  - 시계열 증감 계산 정확: 신한 CFS 자산 Δ=30,705,061,000,000(vs 전기말), 영업이익 Δ=210,239,000,000(vs 전기동기).
  - provenance 팝오버: 계산식·입력 원문(account_id·기간·연결/별도)·결과·`engine:deterministic` 표시.
  - 콘솔 에러 **0**, tablet(768) 가로 오버플로 없음.

- **미완료/다음 작업**:
  - 백엔드 이식(다음 세션): `collect_dart.py`(CFS/OFS 루프) → `src/`, prism compare/coverage/structure-diff + `_notes_for_comparison` **fs_div 파라미터화** → `notes_search.py`, 매트릭스 `catalog.py`, 신규 `fs_compare.py`/`safety.py`.
  - pdf.js vendored, 임베딩 오프라인(HF_HUB_OFFLINE), 안전 게이트 테스트(`test_no_external_cdn.py`, `test_fs_compare.py`).
  - P2 기능(정합 참조·이상치 플래그·회계정책 diff·연결조정 추정), LLM 옵트인, PyInstaller `dist\setup\`.
  - git init 여부(신규 레포 vs prism 내 디렉터리) 사용자 확인 후 초기 커밋.

- **이슈/특이사항**:
  - 임베드 데이터에서 하나·우리 2025Q3는 분기순이익(ProfitLoss) 미포함(원본도 계정명 표기 상이 가능) → "대응 없음" 로직 자연 시연 가능.
  - 프로토타입 미리보기: `python -m http.server 8020 --directory D:/SOURCE/prism-fs/src/static` 또는 파일 직접 열기.

## 2026-06-01 (오후) — 백엔드 전면 구현 (P1~P3 완료)

- **작업 내용**: prism 백엔드 이식 + 신규 엔진. 정적 프로토타입 → 동작 MVP(서버 :8021).
  - **P1**: `collect_dart.py`(R2 적용) + `app.py`(main.py 서브셋, KG/wordmap 제거) 이식. DART 라이브 수집·PDF 저장·PDF inline 서빙·vendored pdf.js 뷰어·매트릭스.
  - **P2**: ko-sroberta 인덱싱 + 주석 검색/비교(`/api/compare·coverage·structure-diff`). `_notes_for_comparison(idx, fs_div)` 파라미터화 + `CompareTarget.fs_div` → 연결/별도 1급, 동일 fs_div끼리만 매칭.
  - **P3**: 신규 `fs_compare.py`(결정론) + `/api/fs/{accounts,delta,consolidated-vs-separate,benchmark,ratio}` + 재무제표 비교 탭(4뷰, provenance 팝오버).
- **변경/신규 파일**: `src/app.py`(KG/wordmap 제거 + fs_div + /api/fs/*), `src/collect_dart.py`(R2 원본명 보존+복제, `download_attachment` httpx 직접 다운로드), `src/fs_compare.py`(신규), `src/static/index.html`(prism UI 그대로 + 미사용 탭 제거 + vendored pdf.js + 재무제표 비교 탭), `src/static/vendor/pdfjs/*`, `tests/*`.
- **버그수정**:
  - **OpenDartReader.download 가 pdf.do URL 에서 0바이트 파일 생성** → httpx 직접 GET(UA/Referer)로 교체(라이브 2.1MB 정상).
  - 경로 CWD 비의존화(`BASE_DIR=__file__`) → preview/uvicorn 어디서든 storage/static 일관.
  - pdf.js 인라인 import 가 bare specifier 라 모듈 해석 실패 → `./vendor/...` 로 수정(workerSrc 정상).
- **검증(라이브 :8021)**: 4사 2026Q1 수집·인덱싱. R2: 원본명+report.pdf 복제본 공존. PDF inline·뷰어 렌더. fs_compare: 자산 Δ 30,705,061,000,000(전기말)·영업이익(전기동기)·부채비율 1228.55%·연결−별도 777조·4사 벤치(KB>신한>하나>우리). 외부 https 0·콘솔 0·**pytest 8 passed**.
- **미완료/다음**: 비교 조회(주석) 탭 fs_div 셀렉터 노출; 별도 주석 태깅 빈약(추출기 특성); 추가 기간 수집; PyInstaller; **git init 미정(사용자 확인)**.

## 2026-06-01 14:17 — UI 축 전환(문서종류→연결/별도) + 주석 RAG (§5.4) — 커밋 `08550f9`

- **작업 내용**:
  - 사용자 지시 "report/review(분기/검토) 문서종류 축 제거, **연결(CFS)/별도(OFS)를 1급 축**으로". 라이브러리 매트릭스 셀을 `[🔗연결 N][🏢별도 N]` pill 2종으로 재구성. 내부 소스는 항상 report.pdf 고정.
  - **주석 RAG(§5.4)** 신규: 자연어 질의 → 동일 fs_div note title 임베딩 cosine 상위 K retrieve → report.pdf 본문 텍스트 추출(노트당 페이지·글자 캡) → 인용 강제 프롬프트로 Ollama 답변. **옵트인**(Ollama off면 retrieval_only) + **출처 칩**.
- **변경 파일**: `src/app.py`(/api/notes/rag, fs_div 1급화), `src/notes_rag.py`(신규), `src/static/index.html`(매트릭스 pill·RAG UI).
- **결정 사항**: AI는 주석 텍스트 전용(숫자 무경유). sources 비면 answer 생성 금지(인용 강제 = 계약). 비교 컬럼/fs_div 매칭 규칙은 P1 그대로 유지.
- **검증**: 매트릭스 연결/별도 pill 클릭=비교 대상. RAG 답변에 출처 라벨 동반. 외부 https 0·콘솔 0.

## 2026-06-01 14:41 — 별도재무제표 주석 추출 활성화 — 커밋 `715616f`

- **이슈**: 매트릭스 별도 pill 이 `–`(0건). 원인 = 추출기가 `"(별도)"` 접미만 앵커로 인식 → 분기보고서 본문의 별도 주석은 접미가 없어 누락.
- **수정**: `_extract_full_report_notes` 에 **별도 섹션 앵커 폴백** 추가(`_find_separate_section_page`). 연결 영역 종료 이후 "재무제표 주석" 섹션부터 무접미 `1.., 2..` 시퀀스를 별도로 인식.
- **검증**: 4사 재인덱싱 → 별도 주석 21~29건 채워짐(연결 25~37). 매트릭스 별도 pill 정상.

## 2026-06-01 15:28 — A. 커버리지·공시갭 탭 (P2) — 커밋 `7d18b5c`

- **작업 내용**: 신한 중심 4사 비교용 **🧭 커버리지·공시갭** 탭 신설. 연결/별도 축 적용. 4섹션 스캐폴드(A.커버리지 매트릭스 B.신한 vs 동종 차집합 C.표준 주제 정렬 자리 D.AI 비교 메모 자리).
- **변경 파일**: `src/app.py`(/api/coverage·structure-diff fs_div·note_kind 필터), `src/static/index.html`(coverage 탭, loadInsight/reloadInsight).
- **검증**: 커버리지 매트릭스 렌더, 신한 vs 동종 차집합 표시.

## 2026-06-01 15:39 — B. 결정론 분석 4종 (P3 확장) — 커밋 `9b1859a`

- **작업 내용**: `fs_compare.py` 에 결정론 4종 추가 — ⑤시계열 추이(다기간 `timeseries`) ⑥이상치 플래그(`flags`, 부호반전·|증감률|≥50%) ⑦연결조정 상세(`consolidated_subtotals`, 자회사효과 비중) ⑧주석 정합참조(`/api/notes/account-refs`, 계정명↔주석 임베딩 후보 제시·확정은 사용자).
- **변경 파일**: `src/fs_compare.py`(timeseries/flags/consolidated_subtotals + TIMESERIES_ACCOUNTS), `src/app.py`(/api/fs/{timeseries,timeseries-accounts,flags,consolidated-subtotals}, /api/notes/account-refs), `src/static/index.html`(재무제표 비교 8뷰로 확장).
- **결정 사항**: 전 항목 provenance 동봉·AI 무경유. 정합참조는 숫자 자동 일치판정 금지(후보만, HITL). 연결조정은 "추정·비감사" 워터마크.
- **검증**: 신한 자산 2025Q2→26Q1 추이(+4%대). 연결조정 자산 자회사효과 95.2%·분기순익 −43.1%. 이상치 신한61·우리78건. 정합참조 영업이익→영업수익 95%.

## 2026-06-01 15:52 — C. 주기(회계정책)/서술형 주석 필터 (P4) — 커밋 `144d156`

- **작업 내용**: `note_filters.py` 신규 — 주석 제목 키워드 룰셋(회계정책·작성기준·추정·판단 등)으로 `주기`/`서술형` 결정론 분류. compare/coverage/RAG 공용 필터(`note_kind` 파라미터).
- **변경 파일**: `src/note_filters.py`(신규), `src/app.py`(compare/coverage/rag note_kind 필터), `src/static/index.html`(주기/서술형 셀렉터), `tests/test_note_filters.py`.
- **검증**: 키워드 분류 단위테스트 통과. 비교 조회·RAG 에서 주기/서술형 필터 동작.

## 2026-06-01 15:57 — C. 표준 주제 매핑 (P5, §5.2) — 커밋 `22a8360`

- **작업 내용**: `note_topics.py` 신규 — 회사마다 번호·제목이 다른 주석을 canonical topic(통제어휘)으로 임베딩 cosine 분류·정렬. 임계(0.45) 미만 "미분류". `/api/notes/topic-map` + 커버리지 탭 C섹션(topic × 4사 정렬표).
- **변경 파일**: `src/note_topics.py`(신규), `src/app.py`(/api/notes/topic-map, _load_topic_dict_topics), `src/static/index.html`(C섹션 렌더), `tests/test_note_topics.py`.
- **결정 사항**: 임베딩 분류는 결정론(AI 생성 무경유). 숫자 무관·주석 제목 텍스트만.
- **검증**: 대손충당금→하나 대손준비금 93% 매칭. 단위테스트 통과.

## 2026-06-01 16:20 — D. AI 비교 메모 초안 (P6, §5.3) — 커밋 `4496899`

- **작업 내용**: `notes_rag.py` 에 `build_compare_prompt`/`answer_compare_ollama` 추가 — 표준 주제별 회사 간 회계정책·추정가정·공시범위 차이 비교 초안. `/api/notes/compare-memo` + 커버리지 탭 D섹션. **옵트인·인용 강제·HITL**(말미 "⚠️ 검토 필요" 강제).
- **변경 파일**: `src/notes_rag.py`(비교 프롬프트), `src/app.py`(/api/notes/compare-memo), `src/static/index.html`(D섹션).
- **결정 사항**: sources 비면 생성 안 함. 숫자 생성·재계산 금지 프롬프트 내장. `tests/test_ai_citation_required.py` 게이트로 인용 강제 보증.
- **검증**: 비교메모 출처 인용 동반 생성(~96초, Ollama). 키 없음 → ollama 폴백.

## 2026-06-01 16:30 — D. OpenAI 옵트인 LLM 경로 (P7) — 커밋 `f29cbb2`

- **작업 내용**: `notes_rag._generate` 에 OpenAI 경로 추가 — `.env OPENAI_API_KEY` 존재 시 OpenAI(gpt-4o-mini) 우선, 실패/부재 시 로컬 Ollama 폴백. 키는 헤더로만 전송(로그·예외 노출 금지). `/api/health` 가 `llm_provider`(openai/ollama/none) 노출.
- **변경 파일**: `src/notes_rag.py`(_generate OpenAI 분기), `src/app.py`(health llm_provider), `_safe_err` 에 OPENAI_API_KEY·`sk-*`·Bearer 마스킹 추가.
- **결정 사항**: 기본은 로컬 Ollama(폐쇄망). OpenAI는 명시적 옵트인. temperature=0.0 결정성 우선.
- **검증**: 키 부재 환경 → ollama 폴백 정상. **pytest 26 passed**. 외부 https 0·콘솔 0. (OpenAI 실호출은 키 부재로 미검증 — D 선택과제.)

## 2026-06-01 (현황 종합) — A~D(P1~P7) 전부 완료·푸시

- **데이터**: 4사 × 3분기(2025Q2·2025Q3·2026Q1) report 인덱싱(연결 25~37 / 별도 21~29). fs_structured CFS/OFS 양쪽. (2025FY 미수집.)
- **탭 5종**: 📚 라이브러리 / 🔍 비교 조회(+RAG·주기/서술형 필터) / 📈 재무제표 비교(결정론 8뷰) / 🧭 커버리지·공시갭(A~D 섹션).
- **테스트**: pytest 26 passed(라이브 포함 ~2분). 서버 :8021 단일 가동.
- **남은 후속**: E(PyInstaller `dist\setup\` + Playwright E2E), F(dormant JS 정리·`safety.py` 마스킹 중앙화), (선택) 2025FY 수집·OpenAI 실호출 검증.
