# prism-fs 백엔드 의존성 (다음 세션 구현 예정 — 현재는 placeholder)
# Windows 11 / Python 3.11 기준

fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.35

# DART 수집 (py3.11 핀 — 0.3.x 는 py3.13 요구)
OpenDartReader>=0.2.2,<0.3
requests>=2.31

# 구조화/검증
lxml>=4.9.0

# 검색 (오프라인 임베딩 + BM25). 부재 시 bigram 폴백.
# sentence-transformers>=2.5
# rank-bm25>=0.2.2
# 한국어 형태소 토크나이저(BM25 정밀도↑). 부재 시 정규식 폴백.
kiwipiepy>=0.17

# 테스트
pytest>=8.0
# E2E(개발 PC 전용) — 선행: python -m playwright install chromium
playwright>=1.40

# 배포 빌드(개발 PC 전용) — onedir 풀번들(torch+ST 동봉)
# pyinstaller>=6.0
